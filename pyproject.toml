[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "meme-text"
version = "1.0.0"
requires-python = ">=3.9"

[project.scripts]
meme-text = "meme_text:main"
